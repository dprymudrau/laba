<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "org/apache/ibatis/builder/xml/mybatis-3-mapper.dtd">
<mapper namespace = "com.solvd.deliveryProject.dao.interfaces.ICarDAO">
    <select id="getById" parameterType ="int" resultMap="carResultMap">
        SELECT  c.cityId as city_cityId,
        c.carName as city_cityName,
        FROM City c
        WHERE c.cityId = #{id};
    </select>
    <resultMap id="CityResultMap" type="com.solvd.laba.binary.City">
        <id column="city_cityId" property="cityId"/>
        <result column="city_cityName" property="city_cityName"/>
    </resultMap>
</mapper>