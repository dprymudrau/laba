<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="app.properties"/>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${db.driver}"/>
                <property name="url" value="${db.url}"/>
                <property name="username" value="${db.username}"/>
                <property name="password" value="${db.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="resources/mapper-Car.xml"/>
        <mapper resource="resource/mapper-City.xml"/>
        <mapper resource="resources/mapper-Drivers.xml"/>
        <mapper resource="resources/mapper-Orders.xml"/>
        <mapper resource="resources/mapper-Streets.xml"/>
        <mapper resource="resources/mapper-Passengers.xml"/>
    </mappers>
</configuration>